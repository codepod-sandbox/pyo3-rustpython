# rpds-py

## Local Branch

- `third_party/rpds-py`: `codepod-sandbox/pyo3-rustpython-migration`

## Current Local Patch

- carry the existing RustPython-compat package-side adjustments in `src/lib.rs` inside the upstream checkout instead of a copied `examples/rpds/src/lib.rs`

## Upstream PR

- not opened yet
